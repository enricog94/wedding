# Legacy Cloudflare D1 migrations

These migrations document the production D1 schema used before the Supabase
PostgreSQL migration. They are intentionally retained for rollback and data
export, but are no longer referenced by the Worker runtime or Wrangler.

The active PostgreSQL schema is versioned under `supabase/migrations/`.
